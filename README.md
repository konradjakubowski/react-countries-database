A Portfolio Project. Countries Database Application build with React, Redux and React-Router in ES6 syntax:

Open the Application manually:
1. Download this repository (ZIP)
2. Unpack the files
3. Inside the folder (with cmd/gitbash):
5. npm install
6. npm run start
